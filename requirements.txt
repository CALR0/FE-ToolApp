# Dependencias externas de FE-Tool
# Instalar con: pip install -r requirements.txt

requests>=2.28.0      # Comunicación HTTP/SOAP con el RNDC (rndc_service, rndc_uploader)
pandas>=1.5.0         # Lectura y procesamiento de archivos Excel (excel_loader, cruzar_remesas, consultar_remesas, extraer_datos_rg)
openpyxl>=3.0.10      # Motor de lectura/escritura .xlsx requerido por pandas
pdfplumber>=0.9.0     # Extracción de datos de PDFs de facturas (extraer_datos_rg)
